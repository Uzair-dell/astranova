#include <stdio.h>
#include <math.h>

void world_print(const char* msg) { printf("%s\n", msg); }

int main() {
    double parallel0 = 0.0;
    parallel0 += 0.0000000000;
    parallel0 += 0.0000000000;
    parallel0 += 0.0000000000;
    printf("%f\n", parallel0);
    printf("result = %f\n", parallel0);
    return 0;
}
